# AutoForge 运行时数据（在 data_dir 中，不在仓库内）
# .autoforge/ 目录可能出现在 workspace_dir 中

# 并行构建临时工作目录
worktrees/

# 种子文件（包含项目特定的隐私信息，绝不入库）
seed.md

# 配置文件（包含项目特定路径，绝不入库；使用 .template.json 作为模板）
autoforge_config.json

# Python 缓存
__pycache__/
*.pyc

# 系统文件
Thumbs.db
.DS_Store

# IDE
.vscode/
.idea/
